name: uas_capstone
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter

  # ===== CORE DEPENDENCIES =====
  http: ^1.1.0
  provider: ^6.0.5
  shared_preferences: ^2.2.0
  intl: ^0.18.1

  # ===== DATABASE =====
  sqflite: ^2.3.0
  path: ^1.9.0

  # ===== CAMERA & IMAGE =====
  image_picker: ^1.0.7

  # ===== CHARTS =====
  fl_chart: ^0.65.0

  # ===== NOTIFICATIONS (FIXED & STABLE) =====
  flutter_local_notifications: ^17.2.2
  timezone: ^0.9.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
