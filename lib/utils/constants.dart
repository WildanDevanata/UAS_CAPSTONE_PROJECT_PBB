// Constants untuk maintainability dan readability
class AppConstants {
  // App Info
  static const String appName = 'WaterLog';

  // Preset amounts (ml)
  static const List<int> presetAmounts = [250, 500, 1000];

  // Default values
  static const double defaultTarget = 2000.0;

  // Database
  static const String dbName = 'waterlog.db';
  static const int dbVersion = 1;
  static const String appVersion = '1.0.0'; // ← Tambah ini
  // API - Ganti dengan URL server Anda
}
